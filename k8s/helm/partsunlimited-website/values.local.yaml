# Default values for PartsUnlimited.Website.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

canaries:
  - name: blue
    replicaCount: 1
    weight: 80
    tag: 1.0.0.16
  - name: green
    replicaCount: 2
    weight: 20
    tag: 1.0.0.16

system:
  name: PartsUnlimited
  environment: Development
  ingressProxyUrl: http://pu-dev-traefik-internal.pu-dev.svc
  discountsBaseAddress: http://pu-api.local

image:
  repository: cdk8spu.azurecr.io/partsunlimited-website
  pullPolicy: IfNotPresent
  pullSecretName: "cdk8spu"  # enter a value if you need a pull secret for your registry

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: true
  path: /site
  hosts:
    - pu-web.local
  class: pu-dev-traefik-external

resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #  cpu: 100m
  #  memory: 128Mi
  # requests:
  #  cpu: 100m
  #  memory: 128Mi

nodeSelector: {}

tolerations: []

affinity: {}
